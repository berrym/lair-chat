# cargo-audit configuration
# See: https://github.com/rustsec/rustsec/tree/main/cargo-audit

[advisories]
# Advisories to ignore (with justification)
ignore = [
    # RUSTSEC-2023-0071: Marvin Attack on RSA
    # This is a transitive dependency from sqlx-mysql which we don't use.
    # We only use SQLite, but sqlx includes MySQL support by default.
    # No fix is currently available upstream.
    "RUSTSEC-2023-0071",
]
